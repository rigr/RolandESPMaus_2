# main/idf_component.yml
# Fixed to use correct manifest syntax for ESP-IDF v4.4

dependencies:
  idf:
    version: ">=4.4.0,<5.0.0"

  # Use built-in classic Bluetooth stack from IDF
  bt:
    public: true

  # Use local esp_hid component shipped within the repo
  esp_hid:
    path: "../components/esp_hid"
